# Uncomment the next line to define a global platform for your project
platform :ios, '14.0' # or later if needed (adjust to your minimum iOS version)

# Firebase pods (updated for modular imports)
pod 'Firebase'
pod 'Firebase/Core'
pod 'Firebase/Firestore', :modular_headers => true
pod 'Firebase/Auth', :modular_headers => true

target 'LoanOrigination' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LoanOrigination (if you have any other pods, list them here)
end
